## 1. Подготовка реализации и привязка приёмки

- [x] 1.1 Найти либо создать отдельную issue B02 по объёму proposal, связать с #1/#14/#6 и добавить номер/ссылку в proposal; проверить, что критерии охватывают единую сборку, синтаксис, диагностику и сохранение черновиков, а состояние issue открыто.
- [x] 1.2 До правок кода выполнить `go test ./internal/configuration/... ./internal/jsonvalue/... -count=1`, зафиксировать исходный результат и соответствие сценариев дельты будущим тестам в `verification.md`; отсутствие нового поведения пока не выдавать за Red.

## 2. Проверка сырого шаблона и разбор строк

- [x] 2.1 Для корневого/вложенного `null` сначала добавить контрактные тесты `SubstituteValues` с путями `""`, `/optional`, `/a~1b~0c/0`; подтвердить поведенческий Red, переиспользовать доменный поиск `null` для Green, после рефакторинга повторить целевые тесты. Включить регрессию постоянных `"null"`, пустой строки и `false`; записать фактические результаты в `verification.md`.
- [x] 2.2 Для `$$`, буквальной ссылки, встроенной буквальной ссылки, `$$$$`, одиночного `$` и экранированного незакрытого начала сначала добавить тесты через `SubstituteValues`; для каждого небольшого сценария выполнить Red → минимальную логику → Green → рефакторинг/повтор. Проверить корень, объект и массив и записать результаты; уже проходящие случаи отмечать как регрессии.
- [x] 2.3 Для частичной интерполяции, соседних ссылок и нечётного числа долларов сначала добавить тесты с присутствующими и отсутствующими именами; подтвердить Red, реализовать классификацию всей строки до поиска значения, получить Green и повторить проверки после рефакторинга. Проверить `$$${host}` → ошибка и `$$$${host}` → буквальная строка, а также отсутствие частичного результата; записать результаты.
- [x] 2.4 Для пустого/незакрытого/недопустимого имени в целой и составной строке сначала добавить тесты, включая низкоуровневый набор с ошибочным именем; подтвердить Red по неверной классификации, обеспечить `invalid_reference` до поиска значения, получить Green и повторить после рефакторинга. Проверить пробел, управляющий символ, `$` и `{` в имени; записать результаты.
- [x] 2.5 До дополнительных изменений сканера добавить тесты JSON-экранированных долларов/имени и обратной косой черты, буквальных ключей и диагностики одной/нескольких ссылок; отсутствующие гарантии провести через Red → Green → Refactor, уже готовые отметить регрессиями. Проверить точный ID, источник, имя только в предусмотренных случаях, корневой/экранированный JSON Pointer, отсутствие исходных фрагментов в ошибках; записать результаты.

## 3. Единая операция разрешения переменных

- [x] 3.1 Сначала добавить тест успешной `AssembleElement` с персональным адресом, дефолтным числовым портом и буквальной ссылкой из дельты; при необходимости создать только компилируемую заглушку, подтвердить поведенческий Red, добавить минимальную композицию B01 и подстановки для Green, затем рефакторинг и повтор тестов. Записать фактический Red отдельно от ошибок компиляции/окружения.
- [x] 3.2 До дальнейших изменений добавить сквозные сценарии B01: обязательное неиспользуемое определение, Unicode White_Space и дефолт, неверный личный тип без fallback, неверный невыбранный дефолт, неверное имя/тип определения, дубликат и лишнее личное имя, точные имена с точкой/регистром/Unicode. Для недостающего поведения провести отдельные Red → Green → Refactor; композицию, проходящую сразу, честно отметить регрессией. Проверить отсутствие результата, источники/пути и `ExpectedType` по контракту подготовки.
- [x] 3.3 Сначала добавить сквозные сценарии строгого JSON и `null` для шаблона, личных значений и дефолтов, включая неиспользуемые и невыбранные данные, пустой присутствующий буфер и отсутствие записи. Для недостающих гарантий выполнить Red → Green → Refactor, остальные отметить регрессиями; проверить соответствующие коды, байтовую позицию от нуля либо путь относительно нужного источника и отсутствие результата.
- [x] 3.4 Сначала добавить тест неизвестной целой ссылки при пустых определениях и проверку независимых вызовов для разных элементов; подтвердить отсутствие автообъявления и общий полный синтаксис через `AssembleElement`. При недостающем поведении выполнить Red → Green → Refactor, уже проходящие случаи отметить регрессиями; входные определения не должны измениться, `unknown_variable` должен указывать точное имя и место ссылки.

## 4. Границы данных и регрессии сохранения

- [x] 4.1 До изменения работы с заменами добавить сценарии буквальных `${...}`, `$$`, некорректных/частичных ссылок внутри string/object/array значений и выбранного дефолта; подтвердить отсутствие повторной обработки через обе операции. Недостающее поведение провести через Red → Green → Refactor, уже проходящее отметить регрессиями; сверить JSON-результаты со сценариями дельты.
- [x] 4.2 Добавить сквозные проверки пяти типов, постоянного шаблона, неизвестных полей, порядка массива и точных чисел `9007199254740993`, длинной дроби и `1e400`; проверить сохранение семантики без сравнения форматирования или порядка ключей. Добавить проверки независимости буфера и неизменности всех входов при успехе/ошибке; отсутствующие гарантии провести через Red → Green → Refactor, остальные зафиксировать как регрессии.
- [x] 4.3 Проверить сценарий «черновик сохраняется и читается, но сборка отклоняется» для `null`, частичной ссылки и незаполненной переменной через публичные операции ядра и `elementjson`; выполнить существующие тесты обнаружения объявлений и точного round-trip. При обнаруженной регрессии сначала воспроизводящий Red, затем минимальное исправление и Green/Refactor. Подтвердить, что общий JSON-парсер не получил запрет `null` и сохранение не стало сборкой.

## 5. Документация и общая проверка

- [x] 5.1 Обновить комментарии функций, README, AGENTS.md, `docs/configuration-model.md`, `docs/architecture.md`, `docs/prototype.md` и `docs/roadmap.md` по фактической реализации: B02 завершает разрешение переменных одного элемента, а межэлементная сборка, метка, Xray и публикация остаются впереди. Проверить ссылки, JSON-примеры и отсутствие частных данных; основной spec пока не менять вручную.
- [x] 5.2 Выполнить `go test ./... -count=1`, `go vet ./...`, `CGO_ENABLED=1 go test -race ./... -count=1`, проверку форматирования и сборки `./cmd/xkeen-ui-server` для Linux amd64/arm64 с `CGO_ENABLED=0` в каталог артефактов; записать точные команды и результаты в `verification.md`. Подтвердить отсутствие новых внешних зависимостей и I/O в ядре; при ограничении среды указать его отдельно от результата теста.
- [x] 5.3 Выполнить `openspec validate complete-element-variable-assembly --strict`, проверить трассировку всех сценариев дельты и применимых A05/B01 к тестам и свести фактические Red/Green, регрессии и ограничения в `verification.md`; проверить каждый критерий отдельной issue B02, не отмечая частичную реализацию завершённой.

## 6. Завершение после реализации

- [x] 6.1 После всех необходимых проверок синхронизировать дельту и архивировать change штатным процессом OpenSpec; проверить наличие требований в основной спецификации и отсутствие активного change. Отдельно закрыть issue B02 только по выполненным критериям и явно сообщить её состояние; обзорную #1 оставить открытой.
