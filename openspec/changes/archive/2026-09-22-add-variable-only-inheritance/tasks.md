## 1. Подготовка реализации

- [x] 1.1 Перед реализацией проверить актуальный трекер и выделить отдельную issue B04 из [обзора #1](https://github.com/SunnyDayDev/xkeen-ui-server/issues/1), если она ещё отсутствует; связать её с этим change, B01/B02/B03/A08 и критериями разделов 2–5. Проверка: номер и ссылка записаны в proposal, критерии не включают B05/B06.
- [x] 1.2 Запустить исходные `go test ./internal/configuration/... -count=1` и `openspec validate add-variable-only-inheritance --strict`; начать `verification.md` с фактическими результатами и матрицей «сценарий дельты → тест». Проверка: исходные сбои отделены от поведенческого Red, все 15 сценариев дельты учтены, ограничение проверки одного элемента отмечено как граница контракта.

## 2. Операция и проверка источника

- [x] 2.1 По сценариям `Inheritance without personal values` и `Absent and empty personal settings use the same defaults` написать тесты через `AssembleInheritedElement` из design: нет записи, правильная запись с nil/пустой картой, результат с UUID/шаблоном, отсутствие записи дефолта во входах. С минимальными объявлениями/заглушкой подтвердить поведенческий Red, затем реализовать базовую композицию B02 для Green, отрефакторить и повторить целевые тесты; записать результаты.
- [x] 2.2 Для `Invalid identity and context fail before assembly` сначала написать отдельные случаи неверного UUID элемента/ссылки, вида/ID владельца, позиции и пустого/невалидного UTF-8 ID шаблона в ссылке, включая отсутствие персональной записи. Подтвердить Red, реализовать проверки до B02 для Green, отрефакторить и повторить тесты. Проверка: ожидаемый код, nil-результат и отсутствие невалидных данных в ошибках.
- [x] 2.3 Для `A different template is not the same source`, `Mismatched element UUID is rejected`, `Empty values do not hide a wrong source` и `Source mismatch precedes variable validation` последовательно написать тест каждого небольшого случая, подтвердить Red, добавить минимальную проверку для Green, отрефакторить и повторить тесты. Проверить также несовпадение обоих компонентов по приоритету из design. Проверка: `source_mismatch`/`element_id_mismatch` вместо дефолтов или ошибок переменных, нет результата и изменений входов.

## 3. Наследование изменений шаблона

- [x] 3.1 Для `Template edit reaches a variable override` написать последовательный тест двух снимков шаблона с тем же UUID и персональным портом: новое неизвестное поле доходит до результата, значение и источник сохраняются. Сначала запустить тест, при поведенческом Red добавить минимальную логику для Green; отрефакторить и повторить проверку. Если композиция B02 уже даёт первоначальный Green, явно записать это, не заявляя Red.
- [x] 3.2 Для `Default changes remain inherited` написать последовательные случаи смены дефолта при отсутствующем значении, строке после trim пустой и заполненном персональном числе. Следовать тест → запуск/Red → минимальная логика/Green → рефакторинг → повторная проверка, либо честно зафиксировать первоначальный Green существующей композиции. Проверка: новый дефолт наследуется, заполненное число остаётся, входы не дополняются и не нормализуются.
- [x] 3.3 Для `Definition type changes preserve the personal value`, `Removed definitions do not silently discard values` и `New required definitions block assembly without losing values` по одному написать тест изменения определений между вызовами; пройти фактический Red/Green/Refactor либо отметить первоначальный Green от B02. Проверка: `type_mismatch`, `unknown_variable`, `missing_value`, включая неиспользуемую обязательную переменную, без частичного результата и потери значений.

## 4. Диагностика и границы данных

- [x] 4.1 Для `Template failures retain their original diagnostic context` и ошибок раздела 3 написать тесты владельца/позиции, источника, имени, ожидаемого типа и пути/смещения. Отдельно проверить ошибку определения, синтаксическую ошибку JSON и `missing_value` без персональной записи. Подтвердить Red отсутствующего контекста, добавить отображение диагностики для Green, отрефакторить и повторить тесты. Проверка: источники `template`/`definition`/`default` указывают шаблон, `value` — роутер; значения-маркеры, JSON и невалидные UUID отсутствуют во всех полях ошибок.
- [x] 4.2 Для `Successful results own their content and preserve JSON data` сначала написать тесты точного большого числа, буквальных `${...}`/`$$` внутри данных, порядка массива и независимости буферов в обе стороны; включить успешный дефолт и персональный объект/массив. Следовать тест → запуск/Red → минимальная логика/Green → рефакторинг → повторная проверка либо записать первоначальный Green. Проверка: нет добавленной `_xkeenUiId`, изменения результата не затрагивают входы, изменения входов не затрагивают готовый результат.
- [x] 4.3 Проверить композиционные регрессии B02 через новую границу: `null` шаблона/значений/дефолтов, неверный дефолт при заполненном значении, лишнее имя, nil-буфер персонального значения, корневой примитив/массив и экранирование. Сначала добавить недостающие сценарии тестов и запустить их; для обнаруженного пробела подтвердить Red и исправить до Green с повторной проверкой. Проверить весь `go test ./internal/configuration/... -count=1`; в отчёте связать первоначальный Green с повторным использованием B02, без заявления о новой реализации этих правил.

## 5. Приёмка и документация

- [x] 5.1 Проверить границы зависимостей и сценарий `Variable-only assembly does not handle a collection`: новая операция не выполняет I/O, не зависит от `elementjson`, HTTP или БД, не принимает полный набор/порядок и не заявляет `duplicate_override` или готовность Xray. Проверка: ревью импортов/сигнатуры и неизменности контрактов `AssembleElement`, `Element`, `elementjson`; вывод записан в verification.
- [x] 5.2 Выполнить проверки кода по существующему CI: форматирование, `go test ./... -count=1`, `go vet ./...`, `go test -race ./... -count=1`, сборки Linux amd64/arm64 с `CGO_ENABLED=0`. Проверка: фактические команды, версии среды и результаты записаны; сбои среды не отмечены как успешные проверки или поведенческий Red.
- [x] 5.3 Обновить статус и описание B04 в `README.md`, `AGENTS.md`, `docs/architecture.md`, `docs/configuration-model.md`, `docs/prototype.md`, `docs/roadmap.md`; сохранить границы B05/B06, хранилища и UI. Проверка: ссылки, JSON-примеры и отсутствие частных данных; все заявления о реализации соответствуют тестам. Макет в этом change не меняется и не считается проверенным.
- [x] 5.4 Дополнить `verification.md` итоговой матрицей всех сценариев, фактическими Red/Green и ограничениями; выполнить строгую валидацию change и основных specs. Проверка: все критерии issue B04 сопоставлены выполненным проверкам, незавершённых обязательных пунктов нет. Синхронизацию/архивирование провести этапом завершения; отдельно проверить закрытие issue B04 по приёмке, оставив обзорную #1 открытой.
