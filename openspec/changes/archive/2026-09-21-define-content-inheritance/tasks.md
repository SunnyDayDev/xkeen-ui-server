## 1. Ревью и учёт контракта B03

- [x] 1.1 Сверить дельту с согласованными переключением/сбросом и критериями B03 из roadmap; провести ревью предложенного независимого исключения. Готово: в отчёте завершения есть сопоставление двух режимов, сброса, локального элемента и сохраняемого исключения с требованиями/сценариями, а статус решений в design соответствует результату ревью; B04–B06 не включены в реализацию этого change.
- [x] 1.2 Оформить отдельную issue B03 в рамках принятого процесса учёта и связать её с обзорной #1, A07 #8, этой дельтой и критериями завершения контракта; дополнить proposal фактическим номером/ссылкой. Готово: issue читается из GitHub, её критерии соответствуют B03, реализации B04–B06 и обзорная #1 не помечены выполненными.

## 2. Согласование предметной документации

- [x] 2.1 После ревью обновить раздел наследования в `docs/configuration-model.md`: текущий источник, активные/неактивные значения, полная замена, сброс и исключения. Готово: нет противоречий с дельтой и существующими контрактами A07/A08/B01/B02; выбор содержимого не назван проверкой готовности Xray, будущая реализация обозначена явно, ссылки и JSON-примеры проверены.
- [x] 2.2 Обновить разделы 6/14/15 `docs/prototype.md` и результат B03 в `docs/roadmap.md`. Готово: согласованные правила отделены от будущего хранения/редактора черновиков, смены профиля и удаления источника; код и макет не объявлены реализованными или обновлёнными, B04 остаётся отдельным следующим шагом.

## 3. Передача контрактов в реализацию

- [x] 3.1 Сверить границы и таблицу передачи в design с дельтой: доменное ядро, существующая сборка B02, буквальная ветвь полной замены, независимый результат, безопасная диагностика и полный набор ID до исключений. Готово: каждому сценарию назначена будущая B04/B05/B06 либо явно отдельная граница B07–B12/C04; не появляются зависимости ядра от HTTP/БД и не расширяется адаптер `elementjson` ради состояния роутера. Для будущего C04 отдельно сохранены интеграционные проверки режима, неактивных значений и исключения после перезапуска.
- [x] 3.2 Проверить передачу TDD/BDD: для каждого небольшого сценария B04–B06 запланированы тест через публичную границу по spec, запуск и подтверждённый поведенческий Red, минимальная логика для Green, рефакторинг и повторная проверка с записью фактических результатов. Готово: отдельно перечислены изменения шаблона, возврат с ошибками, сброс, буквальные строки полной замены, повторные переопределения, локальные коллизии, исключения, неизменность входов и отсутствие данных в ошибках; будущий UI предусматривает Pencil и проверку состояний до тестов/кода и визуальную сверку после. Код и тесты функций в B03 не создаются.

## 4. Итоговая проверка и завершение B03

- [x] 4.1 После ревью и правок документации проверить весь комплект: `openspec validate define-content-inheritance --strict`, локальные ссылки, JSON-примеры, UUID, форматирование и отсутствие частных данных; записать фактические проверки и ограничения в отчёт завершения. Готово: проверки проходят, каждый критерий B03 сопоставлен со сценарием; прямой проверкой охвачены новые неотслеживаемые файлы, результаты не названы Red/Green или проверкой Xray.
- [x] 4.2 Завершить B03 отдельным этапом архивирования: синхронизировать принятую capability, архивировать change и отдельно проверить критерии выделенной issue B03 перед закрытием. Готово: итоговый отчёт указывает фактические состояния основной spec, архива и issue; обзорная #1 остаётся открытой, реализация B04 не начинается автоматически.
