## 1. Ревью и завершение контракта A07

- [x] 1.1 Проверить подготовленный контракт по решениям обсуждения: внешний UUID, независимые `tag`/`ruleTag`, единая `_xkeenUiId`, области владельцев, переопределения, коллизии и неподдерживаемые позиции. Готово: результаты ревью и статус предлагаемых технических деталей отражены в design, каждый критерий issue #8 сопоставлен с требованиями и сценариями; реализация A08 не включена в объём A07.
- [x] 1.2 Сверить таблицу совместимости с фактической проверкой в разделе 5 design. Готово: десять позиций и два контрпримера согласованы со spec; версия, синтетические входы, команда, коды завершения и пределы доказательств перечислены. При изменении позиции, формата метки или версии повторить только затронутые пробы на изолированном стенде; неизменный эксперимент повторять не требуется.

## 2. Согласование предметной документации

- [x] 2.1 После ревью обновить разделы элементов, наследования и тегов в `docs/configuration-model.md` по принятому контракту, дать ссылку на capability и сохранить явное отсутствие реализации модели/проекции. Готово: UUID, дубликаты, изменение тегов, конфликт `_xkeenUiId` и ограниченная вставка описаны без противоречий; JSON и локальные ссылки проходят проверку.
- [x] 2.2 Обновить понятия и разделы 5/6/8/15 `docs/prototype.md`, добавить результат A07 в `docs/roadmap.md`. Готово: идентичность больше не обозначена полностью открытым вопросом; копирование/импорт, граф зависимостей, порядок и будущее UI остаются отдельными задачами; макеты не объявлены обновлёнными, A08 не объявлена реализованной.

## 3. Передача в последующие задачи

- [x] 3.1 Сверить таблицу передачи в разделе 6 design с A08 и задачами B/C из roadmap. Готово: модель и round-trip переданы A08, переопределения и совмещённые наборы — B03–B06, проекция и валидация полного конфига — B09–B12, генерация при создании — будущему создающему сценарию; для границ заданы входы, результаты и безопасные ошибки без БД/HTTP в ядре.
- [x] 3.2 Проверить план TDD/BDD для будущей реализации каждой границы: сценарий → контрактный тест → подтверждённый Red → минимальная логика и Green → рефакторинг и повторная проверка с фактическим отчётом. Готово: отдельно учтены невалидный UUID, round-trip, дубликаты/переопределения, идемпотентная метка, конфликт после подстановки, неизменность входов, словари и профиль совместимости; текущие проверки Xray не названы Red/Green нашего кода. Для будущего UI сохранён порядок «Pencil и визуальная проверка → тесты → код → визуальная сверка».

## 4. Итоговая проверка и завершение

- [x] 4.1 Проверить окончательный комплект после ревью и правок документации: `openspec validate define-element-identity --strict`, JSON-примеры, UUID, локальные ссылки, отсутствие частных данных и форматирование всех новых/изменённых файлов. Готово: результаты и ограничения записаны в отчёте завершения, `git diff --check` дополнен прямой проверкой ещё не отслеживаемых файлов; тесты функций не заявлены выполненными.
- [x] 4.2 Завершить A07 по workflow архивирования: синхронизировать принятую capability в основные specs, архивировать change, отдельно сверить все критерии issue #8 и закрыть её только при полном выполнении. Готово: итоговый отчёт отдельно указывает состояние архива и issue; A08 остаётся следующей самостоятельной реализацией и не запускается автоматически.

## 5. Результат подготовки предложения

2026-09-08: созданы proposal, delta spec, design и tasks. `openspec validate define-element-identity --strict` прошла. Проверены четыре Markdown-файла, семь локальных ссылок, три JSON-блока, девять вхождений корректных UUIDv4 и наличие WHEN/THEN во всех 25 сценариях семи требований. Восемь задач завершения A07 остаются невыполненными.

Прямая проверка новых файлов не нашла завершающих пробелов, отсутствующей конечной новой строки, локальных пользовательских путей и типичных префиксов токенов/приватных ключей. `git diff --check` завершился с кодом 0; файлы ещё не отслеживаются, поэтому их форматирование проверено отдельно. В design записаны 13 фактических проверок Xray с ожидаемыми результатами, включая отказ для словаря уровней.

Проверки относятся к подготовленному предложению и не заменяют итоговую сверку после ревью и обновления общей документации. Код функций и тесты Red/Green не создавались и не запускались. Основная spec ещё не синхронизирована, change не архивирован, issue #8 открыта до выполнения остальных критериев A07.

## 6. Итог выполнения A07

2026-09-08 выполнены все восемь задач. Основная спецификация синхронизирована, change архивирован, issue #8 закрыта как выполненная. Результаты проверок и границы реализации — в [отчёте](verification.md). Раздел 5 сохраняет состояние на момент подготовки предложения.
