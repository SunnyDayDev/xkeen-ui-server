## 1. Привязка задачи и исходная проверка

- [x] 1.1 Перед кодом проверить наличие issue B01; если она ещё отсутствует, выделить её из #1 с критериями этой дельты, зависимостями #7/#9 и согласованной границей `null`. Добавить её номер/ссылку в proposal. Проверка: issue доступна, содержит ссылку на change и проверяемые критерии; #1 остаётся обзорной.
- [x] 1.2 Зафиксировать исходный результат `go test ./internal/configuration/... -count=1` на toolchain из `go.mod` в `verification.md`. Проверка: тесты запускаются; сбой окружения явно отделён от Red нового поведения.

## 2. Контракт и определения

- [x] 2.1 Для `Empty definitions and values` написать тест через предложенный `PrepareValues`; при необходимости добавить только компилируемую заглушку. Подтвердить Red по отсутствующему поведению, реализовать пустой успешный набор, выполнить Green, рефакторинг и повтор целевых тестов. В `verification.md` записать фактические запуски и результаты.
- [x] 2.2 Для `Invalid and duplicate definitions` последовательно провести TDD-циклы: тест → Red → минимальная проверка имени/UTF-8, типа и дублей → Green → рефакторинг и повтор. Включить недопустимые пробельные/управляющие символы, `$`, `{`, `}`, одинаковые повторные определения и неверный тип; проверить коды, `definition` и пути `/i/name`, `/i/type`. Результаты записать в отчёт.
- [x] 2.3 Для `Exact names and independent elements` и `Unicode names are not normalized` последовательно провести тест → Red → Green → рефакторинг и повтор: точка, регистр, кириллица, разные представления Unicode, разные элементы и лишнее персональное имя. Проверка: ровно объявленные имена; лишнее имя даёт `unknown_variable/value`, определения не создаются; Red/Green записаны.

## 3. Выбор персональных значений и дефолтов

- [x] 3.1 Для `Missing values and Unicode whitespace select defaults` и `Unused definition still needs a value` провести по небольшим случаям тест → Red → Green → рефакторинг и повтор. Проверить отсутствие, `""`, пробелы/табуляцию/переносы, U+00A0, пустой дефолт для всех пяти типов и обязательность неиспользуемой переменной. Проверка: нужный дефолт либо `missing_value/value`, без частичного набора; результаты в отчёте.
- [x] 3.2 Для `Whitespace is preserved unless the entire string is missing` и `False zero and empty containers are filled` провести тест → Red → Green → рефакторинг и повтор отдельно для сохраняемых строк и остальных типов. Проверить U+200B, пробелы непустых строк, вложенные пустые строки, `false`, `0`, `{}`, `[]` как персональные значения и дефолты. Проверка: содержимое и типы сохранены; фактический Red/Green записан.
- [x] 3.3 Для `Wrong personal type does not fall back` и `Invalid unused default is still checked` провести тест → Red → Green → рефакторинг и повтор, добавив `ExpectedType` в `Issue` по design. Проверить матрицу несовпадений пяти типов без преобразований, невыбранный ошибочный дефолт и пустой строковый дефолт для number. Проверка: правильные `value/default`, `type_mismatch`, ожидаемый тип и отсутствие результата; отчёт дополнен.
- [x] 3.4 Для `Data remains literal and numbers remain exact` сначала написать проверки точного математического значения и буквальности данных, выполнить Red новых ещё отсутствующих гарантий, затем минимальную логику, Green и рефакторинг с повтором. Если случай уже проходит за счёт предыдущего шага/существующего парсера, честно записать его как регрессионный, а не Red. Проверка: большое целое, точная дробь, `1e400`, `${other}` и `$$` внутри выбранных данных не искажаются.

## 4. Строгий JSON и null

- [x] 4.1 Для `Present but invalid JSON is not missing` и `All defaults are parsed strictly` провести отдельные тест → Red → Green → рефакторинг и повтор. Включить отсутствующий ключ, присутствующие `nil`/пустой буфер, отсутствие дефолта (`nil`) и ненулевой пустой дефолт, UTF-8, хвост после корня, синтаксис и повторные декодированные ключи; ошибочный дефолт проверять при корректном персональном значении. Проверка: код, источник, путь/позиция и отсутствие fallback для повреждённых данных; результаты в отчёте.
- [x] 4.2 Для `Root null never uses a default` провести тест → подтверждённый Red → минимальный запрет корневого `null` → Green → рефакторинг и повтор. Проверить оба источника, наличие корректного альтернативного значения, `null_not_allowed` вместо несовпадения типа, пустой корневой путь и допустимость строки `"null"`. Записать Red/Green.
- [x] 4.3 Для `Nested null and unselected defaults are rejected` провести тест → Red → доменный обход вложенных объектов/массивов → Green → рефакторинг и повтор. Включить `[null]`, смешанную вложенность, `/a~1b~0c/0`, невыбранный дефолт и несовпадение корневого типа в данных с `null`. Проверка: запрет не внесён в общий JSON-парсер; правильные путь и источник, без результата; Red/Green записаны.

## 5. Диагностика, владение данными и композиция

- [x] 5.1 Для `One failure prevents partial output` и `Diagnostics identify definitions and hide data` написать тесты по контракту до недостающей логики, подтвердить Red, выполнить минимальный Green, рефакторинг и повтор. Проверить ID, имя, источник, место определения, путь/позицию, ожидаемый тип и отсутствие демонстрационных значений в полной структуре ошибок; сравнить входы до/после ошибки. Уже проходящие проверки отметить как регрессионные. Отчёт содержит фактические результаты.
- [x] 5.2 Для `Result buffers are independent` провести тест → Red → независимое копирование map и каждого выбранного буфера → Green → рефакторинг и повтор. Проверить обе стороны изменений, объект из персональных данных, массив из дефолта, исходные определения и общий входной буфер у нескольких переменных. Проверка: ни результат, ни входы не меняются через aliasing; Red/Green записаны.
- [x] 5.3 Проверить композицию по `Prepared defaults feed whole value substitution` и `Preparation does not interpret stored content`, а также A08 round-trip черновика с `null` в дефолте и последующий отказ B01. Сначала тесты, затем запуск; при обнаружении недостающей логики — Red → минимальный Green → рефакторинг и повтор, иначе записать регрессионный PASS. Проверка: композиция A06 на корректных целых ссылках работает, A08 продолжает сохранять черновики, B01 не проверяет сырой шаблон и не объявлен полной сборкой.

## 6. Приёмка и документация

- [x] 6.1 Выполнить `go test ./... -count=1`, `go vet ./...`, `go test -race ./... -count=1`, проверить форматирование и обе Linux-сборки без cgo (`amd64`, `arm64`) по существующему CI. Проверить зависимости изменённого ядра: нет БД, HTTP, файловой системы или новых внешних библиотек. Проверка: результаты команд и ограничения окружения записаны в `verification.md`; неизменённые A06/A08 проходят регрессии.
- [x] 6.2 Обновить README, `AGENTS.md` и затронутые описания состояния в `docs/architecture.md`, `docs/configuration-model.md`, `docs/prototype.md`, `docs/roadmap.md`: B01 реализует подготовку и `null` данных; B02 сохраняет сырой шаблон и полный синтаксис; полная сборка/публикация остаётся будущей. Проверка: локальные ссылки и JSON-примеры корректны, частных данных нет, формулировки совпадают с фактическим кодом и дельтой.
- [x] 6.3 Сопоставить все сценарии дельты и критерии выделенной issue B01 с тестами/результатами в `verification.md`, выполнить `openspec validate add-variable-value-preparation --strict`. Проверка: ни один критерий не объявлен выполненным без доказательства; итог явно сообщает состояние issue и change. Закрытие issue после приёмки и синхронизация/архивирование change рассматриваются как отдельные действия завершения; обзорная #1 остаётся открытой.
