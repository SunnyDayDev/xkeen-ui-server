## 1. Проверка полного порядка

- [x] 1.1 По сценариям B07 `Initial order follows template`, `Shared and local rules share one list`, `Empty order is valid` и `Default router follows template move` написать тесты публичной доменной границы, подтвердить поведенческий Red, реализовать минимальную сборку полного/видимого порядка, получить Green и повторить тесты после Refactor. Проверить различие отсутствующего персонального списка и сохранённого пустого списка.
- [x] 1.2 По сценариям B07 об исключённом UUID, повторе, пропуске и отсутствующем локальном правиле написать отдельные тесты, подтвердить Red, добавить проверку полного снимка до фильтрации, получить Green/Refactor. Проверить `duplicate_order_element` и `order_element_not_found`, отсутствие частичного результата и возвращение исключённого UUID на прежнюю позицию после снятия исключения.
- [x] 1.3 По сценариям B07 для некорректного владельца, неканонического UUID и безопасной диагностики написать тесты через новую границу, подтвердить Red нового поведения либо зафиксировать первоначальный Green действующих проверок идентичности, затем повторить проверки после Refactor. Проверить владельца/позицию/валидный UUID в ошибках, отсутствие сырого JSON и невалидного UUID.

## 2. Изменения порядка как независимые кандидаты

- [x] 2.1 По сценариям B07 `Insert at beginning, middle, and end` и повторного UUID написать тесты, подтвердить Red, реализовать вставку локального правила в полный список на индексы 0..len и получить Green/Refactor. Проверить отказ для индекса -1 и len+1, отсутствие мутации входа и создание персонального порядка.
- [x] 2.2 По сценариям B07 `Move shared rule across a local rule`, `Move uses resulting index` и ошибочного индекса написать тесты, подтвердить Red, реализовать перенос после изъятия и получить Green/Refactor. Проверить неизменность UUID, происхождения, содержимого, исключений и относительного порядка остальных правил.
- [x] 2.3 По сценариям B07 добавления X, сдвига локального, удаления B, пакетного X/A/Y/C и перестановки старых общих UUID написать тесты, подтвердить Red, реализовать согласование прежнего и нового шаблонов с проверкой старого полного списка и получить Green/Refactor. Проверить детерминированную вставку по новым индексам, сохранение персональной перестановки и следование шаблону при отсутствии персонального списка.
- [x] 2.4 По сценариям B07 `Reset keeps a local numeric slot` и `Reset retains exclusion and content override` написать тесты, подтвердить Red, реализовать возврат общих UUID в их слоты и получить Green/Refactor. Проверить неизменность локальных индексов, JSON, персональных значений, полной замены и исключения.

## 3. Соединение порядка с выбором B06

- [x] 3.1 По сценариям дельты `Inherited and local content follow ID order` и `Full replacement moves without reassembling` написать тесты, подтвердить Red, соединить проверенный порядок с единственным вызовом B06 и получить Green/Refactor. Проверить буквальный локальный JSON, актуальное наследование, полную замену и отсутствие повторной интерпретации выбранного JSON.
- [x] 3.2 По сценариям дельты `Excluded rule keeps only its full-order position`, `Content failure prevents ordered result` и `Order failure prevents ordered result` написать тесты через новую границу и зафиксировать первоначальный Green уже работающей композиции либо подтвердить Red нового поведения; после Refactor повторить проверки. Проверить пропуск содержимого исключённого правила, сохранение B06 ошибок и nil результата при любой ошибке.
- [x] 3.3 По сценарию дельты `Result and source data are independent` и B07 `Inputs and result are independent` написать тесты мутации входных списков, JSON и результата, подтвердить Red, обеспечить независимые буферы/списки и получить Green/Refactor. Проверить, что повторный вызов и другие результаты не изменились.

## 4. Приёмка и согласование статусов

- [x] 4.1 Сопоставить применимые к B08 сценарии основной B07 и все 6 сценариев дельты с тестами в `verification.md`; сценарии `Save gates publication of changed orders` явно отнести к C05–C07. Записать фактические поведенческие Red/Green и первоначальные Green отдельно. Проверить `go test ./internal/configuration/... -count=1`, `go test ./... -count=1`, `go test -race ./... -count=1`, `go vet ./...` и сборку для Linux amd64/arm64 без cgo; результаты и ограничения занести в отчёт. Проверить, что ядро не получило зависимостей от БД, HTTP, агента и Xray; новых инфраструктурных адаптеров в B08 нет.
- [x] 4.2 После реализации обновить статусы B08 и оставшиеся границы в README.md, AGENTS.md, docs/architecture.md, docs/configuration-model.md, docs/prototype.md и docs/roadmap.md. Проверить, что B08 описан как доменная сборка, а хранение C04, публикация C05–C07 и рабочий UI не объявлены реализованными; макет B07 не менять без новой UI-задачи.
- [x] 4.3 Выполнить `openspec validate assemble-router-rule-order --strict`, проверить локальные ссылки, примеры, UUID и отсутствие частных данных в затронутых материалах, включая новые файлы. Сверить все критерии [issue #27](https://github.com/SunnyDayDev/xkeen-ui-server/issues/27) с фактическими результатами, отдельно проверить статус синхронизации/архивирования change и закрытия issue и зафиксировать их в отчёте.
