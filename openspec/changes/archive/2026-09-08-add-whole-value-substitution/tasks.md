## 1. Подготовить границу тестирования

- [x] 1.1 Обеспечить доступ к закреплённому Go 1.27.1 по инструкции каркаса, записать `go version` и выполнить исходный `go test ./... -count=1`; отделить ограничения SDK/loopback от ошибок поведения в `verification.md`.
- [x] 1.2 Добавить первый внешний тест `configuration_test` для сценария `Constant template needs no values`, минимальные декларации `SubstituteValues`/`Issue` и запускаемую заглушку; подтвердить Red из-за отсутствия результата, затем реализовать минимальный разбор и возврат нового JSON для Green, выполнить рефакторинг и повторный целевой запуск. Проверить пустую и nil map, сохранить фактические результаты в `verification.md`.

## 2. Реализовать подстановку готовых значений

- [x] 2.1 Для `Root substitution preserves the supplied type` по небольшим случаям добавить тесты строки, числа, boolean, объекта и массива через публичную границу; для нового поведения подтвердить Red, реализовать минимум для Green и повторить после рефакторинга. Проверить `"443"`, `0`, `false`, `{}` и `[]` без преобразований или потерь; записать фактический результат каждого цикла.
- [x] 2.2 Для `Prepared values retain all five types` и `Nested references and unknown fields are preserved` последовательно пройти тест → Red → минимальная логика → Green → Refactor с повторным запуском: сначала поля объекта, затем вложенные объекты/массивы. Проверить неизвестные поля, константы, повторную ссылку и порядок массива; записать результаты.
- [x] 2.3 Для `Exact names and element isolation` добавить целевые тесты полного имени `server.address`, отличия регистра и двух независимых наборов. Для отсутствующего полного имени использовать ожидаемый `unknown_variable`; подтвердить Red на отсутствующем поведении, реализовать Green и повторить тесты после рефакторинга, записав результаты.
- [x] 2.4 Для `Numbers in templates and values remain exact` добавить независимые от float64 проверки большого целого в константе и переменной, длинной дроби и `1e400`. Запустить до изменения логики; при Red сохранить причину, реализовать сохранение числового представления до Green и повторить после рефакторинга. Если ранее выбранный парсер уже обеспечивает случай, зафиксировать исходный Green и проверить чувствительность точного сравнения к округлённому значению, не приписывая выполненный Red.

## 3. Проверить входной JSON и диагностику

- [x] 3.1 Для `Invalid template has a safe position` и `Invalid supplied value is identified` отдельно добавить случаи пустого/незавершённого JSON, второго корневого значения и лишнего текста, включая неиспользуемое подготовленное значение. Каждый новый случай сначала запустить, подтвердить Red по отсутствующей проверке/диагностике, затем реализовать минимум для Green и повторить после рефакторинга. Проверить код, ID, источник, имя значения и позицию, а также отсутствие результата; записать результаты.
- [x] 3.2 Для `Duplicate keys are detected after decoding` добавить тесты обычного и Unicode-экранированного дубликата в корне и вложенном объекте шаблона/значения; подтвердить Red, реализовать проверку до записи в map, добиться Green и повторить после рефакторинга. Одновременно проверить `Keys in separate objects do not collide` и путь к дубликату; записать результаты.
- [x] 3.3 Для `Unknown variable identifies a nested reference` и `Root and escaped paths locate unknown references` последовательно добавить тесты вложенного пути, пустого пути корня и `/a~1b~0c/0`. Подтвердить Red по недостающим полям диагностики, реализовать Green, затем проверить после рефакторинга код/ID/источник/точное имя/путь и отсутствие результата; записать результаты.
- [x] 3.4 Для `Error preserves all inputs and does not disclose data` и `Successful result does not share writable input data` добавить проверки исходных байтов шаблона и всех значений при успехе и ошибке, включая изменение успешного корневого результата. Проверить отсутствие демонстрационного секрета и сырого JSON во всех формах выдаваемой диагностики, в том числе ошибках парсера. Запустить тесты до правок, исправлять только подтверждённые Red короткими Green/Refactor циклами; если гарантия уже обеспечена, записать исходный Green без выдуманного Red.

## 4. Сверить архитектуру и существующие проверки

- [x] 4.1 Проверить импорты и diff: `internal/configuration` зависит только от стандартной библиотеки, отсутствуют I/O и изменяемое глобальное состояние, server handler не подключает частичную подстановку. Сверить отсутствие определений/дефолтов в интерфейсе и отсутствие тестов, закрепляющих пропуск запрещённых полной сборкой входов как продуктовый успех; записать результат ревью границ A06/B01/B02.
- [x] 4.2 Выполнить `gofmt -l cmd internal`, `go test ./... -count=1`, `go vet ./...` и `go test -race ./... -count=1`; собрать сервер для Linux AMD64/ARM64 с `CGO_ENABLED=0` по действующему CI. Отдельно выполнить `go test -c` для `./internal/configuration` с `CGO_ENABLED=0`, `GOOS=linux` и каждой из двух архитектур, сохранив бинарники в `.build/`. Записать команды, версии и результаты; не называть кросс-компиляцию запуском на Entware.

## 5. Документировать результат реализации

- [x] 5.1 Обновить README, `docs/configuration-model.md` и `docs/roadmap.md`: отметить фактически реализованный шаг A06 и его вызов из ядра, сохранить отдельный статус будущих B01/B02 и отсутствие HTTP/UI/хранилища. Проверить локальные ссылки, JSON-примеры и отсутствие частных данных; полный контракт A05 не ослаблять.
- [x] 5.2 Завершить `verification.md` с соответствием критериев [issue #7](https://github.com/SunnyDayDev/xkeen-ui-server/issues/7) сценариям и фактическим Red/Green, результатам общих проверок и ограничениям. Выполнить `openspec validate add-whole-value-substitution --strict` и `openspec validate --specs --strict`, сверить delta с основной capability и `git diff --check`. Перед завершением явно указать оставшиеся задачи и статусы issue/архива; закрывать issue только после всех критериев, архивирование выполнить отдельным этапом завершения.
