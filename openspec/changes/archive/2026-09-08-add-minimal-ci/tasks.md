## 1. Правила и подготовка проверки

- [x] 1.1 Закрепить в `AGENTS.md` и применимых rules `openspec/config.yaml` разделение продуктовых specs, процессов и архитектурных принципов: TDD/BDD, DDD, Clean Architecture и SOLID остаются правилами; для чисто процессных changes используется `skip_specs`. Проверить согласованность формулировок с `docs/architecture.md`, чтение конфигурации CLI и отсутствие новых/изменённых продуктовых specs.
- [x] 1.2 До написания workflow подготовить в `verification.md` таблицу сценариев из design §5 с ожидаемыми результатами и пустыми фактическими результатами. Зафиксировать, что это BDD-проверка CI, а существующий сервер повторно не разрабатывается test-first; отсутствие workflow и сбой окружения не засчитываются как Red.

## 2. Минимальный workflow

- [x] 2.1 Разрешить полные SHA стабильных выпусков официальных checkout/setup-go по release tags, записать версии и источники в `verification.md`; проверить поддержку `go-version-file` с директивой toolchain и выбранного GitHub-hosted runner.
- [x] 2.2 Создать `.github/workflows/ci.yml`: один job `checks`, `ubuntu-24.04`, лимит 15 минут, push веток и pull_request без фильтра путей, `contents: read`, `persist-credentials: false`, Go из `go.mod`, `GOTOOLCHAIN=local`, вывод версии и отключённый кеш. Проверить YAML через `actionlint` и вручную проверить события, SHA actions, полномочия и отсутствие secrets/publishing.
- [x] 2.3 Добавить отдельные шаги проверки форматирования и `sh -n scripts/run-server.sh`. На временной копии проверить отказ при нарушенном форматировании с выводом пути и без изменения файла, затем восстановление с кодом 0; исправить обработку exit codes при необходимости и повторить обе проверки. Записать фактические результаты.
- [x] 2.4 Добавить существующие `go test ./... -count=1 -v`, `go vet ./...`, `go test -race ./... -count=1 -v` и обе Linux-сборки без cgo из design §3. Выполнить команды локально в подходящем окружении; проверить коды завершения, наличие обоих бинарников и явный `CGO_ENABLED=1` для race. После упрощения workflow повторить `actionlint`; Go-поведение и новые тестовые интерфейсы не добавлять.

## 3. Документация и проверка на GitHub

- [x] 3.1 Создать отдельный `docs/ci.md` с событиями, шагами, локальным повторением, полномочиями, диагностикой, границами ARM64/race-проверок и защитой main/master по design §6. Добавить ссылки из `README.md` и `docs/server-skeleton.md`, отразить A04 в `docs/roadmap.md` без преждевременной отметки завершения. Проверить локальные ссылки, команды, отсутствие частных данных и `git diff --check`.
- [x] 3.2 На ветке/PR реализации получить фактические успешные GitHub runs для push и pull_request: все обязательные шаги выполнены, версия Go соответствует toolchain, обе сборки прошли. Записать ссылки, event, commit SHA, runner и исходы в `verification.md`; локальный успех не заменяет этот пункт.
- [x] 3.3 После успешных runs взять фактическое имя check и integration ID GitHub Actions; создать или обновить активный ruleset main/master по design §6: PR, 0 обязательных approvals, required check с актуальной базой, запрет force push/удаления, без bypass. Перечитать ruleset и эффективные правила main и master, сохранить относящиеся к защите параметры и URL в отчёте; workflow сохраняет минимальные права.
- [x] 3.4 На проверочной версии временно изменить только ожидание существующего `TestHealth`, сохранив компиляцию и форматирование; сначала убедиться в локальном падении по ожидаемой причине, затем получить неуспешный GitHub run именно на Tests. Зафиксировать имя теста, диагностику, итог failure и блокировку слияния PR обязательным check. Ошибка SDK/сети/YAML не удовлетворяет проверке отказа.
- [x] 3.5 Восстановить исходное ожидание, проверить отсутствие тестовой мутации в итоговом diff и получить успешные GitHub checks окончательного состояния и снятие блокировки обязательного CI в PR. Записать связь отказа и восстановления; после любой последующей правки workflow повторить затронутую проверку. Проверочные изменения не включать в итоговый код.

## 4. Завершение реализации

- [x] 4.1 Дополнить `verification.md` фактическими локальными и GitHub результатами, версиями инструментов, SHA actions, состоянием защиты веток и ограничениями. Сверить критерии issue #5 и дополнительный запрос защиты main/master с доказательствами; выполнить `openspec validate add-minimal-ci --strict --json`, проверку ссылок и `git diff --check`. При недоступности GitHub явно оставить соответствующие задачи незавершёнными.
- [x] 4.2 После выполнения всех критериев актуализировать статус A04 в документации, архивировать change через OpenSpec с `skip_specs: true` и отдельно закрыть issue #5. Проверить наличие архива, отсутствие процессных delta specs и фактический статус issue; в итоговом отчёте явно указать оба состояния и причины оставшихся ограничений.
