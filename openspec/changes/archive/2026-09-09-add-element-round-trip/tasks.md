## 1. Часть A08.1 — модель и точное восстановление записи

- [x] 1.1 Зафиксировать исходную проверку `go test ./internal/configuration -count=1` на toolchain из `go.mod` и завести `verification.md` с сопоставлением сценариев двум дельтам и A07. Разделять результаты A08 и будущие проверки полной сборки B01/B02; критерий — записаны фактический результат и границы, сбой окружения не назван Red.
- [x] 1.2 Для `Reading and updating retain external identity` написать тесты восстановления с каноническим UUID и затем по одному случаю отсутствия/невалидного UUID через предполагаемую границу ядра. Для каждого нового поведения: запустить тест и подтвердить Red после минимальных объявлений, реализовать минимум для Green, выполнить рефакторинг и повторную проверку; записать коды `invalid_element_id` и отсутствие генерации/результата в отчёт.
- [x] 1.3 Проверить изменение с прежним UUID и отказ `element_id_mismatch` для другого UUID отдельными циклами «тест → подтверждённый Red → минимальная реализация → Green → рефакторинг/повтор». Проверка: смена JSON/тега сохраняет ID, отказ не меняет исходный элемент; фактические результаты записаны.
- [x] 1.4 Реализовать чтение/запись оболочки из design по сценарию `Exact source round trip`: сначала тест на байтовое равенство текста с CRLF, пробелами, порядком ключей, `\u0061`, `1e+03` и большим числом; подтвердить Red, довести минимальный адаптер до Green, отрефакторить и повторить тесты. Проверка сравнивает исходный текст, а не повторно сериализованное дерево; результат цикла записан.
- [x] 1.5 Отдельными малыми циклами «тест → Red → минимум → Green → рефакторинг/повтор» покрыть `Definitions and optional defaults round trip` и `Content shape and marker remain untouched`: пять типов, отсутствие/пустой/null-дефолт, точность дефолтов, корневые примитивы/массивы и нетронутая `_xkeenUiId`. Проверка: UUID и определения восстановлены, текст точный; фактические результаты в отчёте. Обнаружение ссылок добавляется в части 2.
- [x] 1.6 Для `Stored records enforce structural integrity` последовательно написать тесты неверной оболочки/полей, пустого и незавершённого JSON, второго корня, неверного UTF-8, дублей ключей оболочки/содержимого/дефолта после декодирования и допустимых одинаковых ключей в разных объектах. Каждый сценарий проходит Red → минимальную логику → Green → рефакторинг/повтор; проверены безопасные код/источник/позиция и отсутствие результата. Если общий парсер вынесен, повторить существующие тесты A06 до/после и записать результаты.

## 2. Часть A08.2 — автоматические объявления и сохранение настроек

- [x] 2.1 Через `PrepareElement` и сохранение написать тест `A whole reference declares a string with empty default`: `${port}` и `${options}` получают `string`/`""`. Подтвердить Red, добавить минимум логики до Green, отрефакторить и повторить; проверить неизменность исходного текста и отсутствие угадывания типа, записать результат.
- [x] 2.2 Последовательно проверить корень/вложенность и повторные ссылки, точные имена с точками/регистром/Unicode и независимость блоков. Для каждого малого сценария: тест → подтверждённый Red → минимум для Green → рефакторинг и повтор; проверить одно новое определение на точное имя и отсутствие Unicode-нормализации, записать результаты.
- [x] 2.3 Проверить распознавание JSON-экранированной целой ссылки и исключение ключей, дефолтов, `$${host}`, `$$${host}`, частичных и невалидных ссылок. Каждый новый случай провести через тест → Red → минимальную реализацию → Green → рефакторинг/повтор; критерий — точные множества объявлений без изменения исходника, а не реализация полной сборки B02. Записать результаты.
- [x] 2.4 Для существующего определения с `number`/`443`, смены типа с пустым дефолтом и повторного сохранения написать тесты сохранения настроек и идемпотентности. Для каждого сценария подтвердить Red, реализовать минимум, получить Green и повторить после рефакторинга; проверить сохранение порядка прежних записей и отсутствие сброса или дублей, записать результаты.
- [x] 2.5 Отдельными циклами «тест → Red → минимум → Green → рефакторинг/повтор» покрыть удаление/возврат последней ссылки, неиспользуемое определение, явное удаление при отсутствии и наличии ссылки, переименование без переноса настроек. Критерий — сохранённые определения точно соответствуют сценариям дельты; записать фактические результаты, не реализуя персональную миграцию.
- [x] 2.6 Проверить `Reading does not normalize a stored record` и сохранение семантически ошибочных определений: чтение не добавляет имена, сохранение дополняет отсутствующие, существующие дубликаты/неизвестные типы не исправляются. Для каждого нового поведения выполнить тест → подтверждённый Red → минимум → Green → рефакторинг/повтор; проверить отсутствие побочных изменений, записать результаты.

## 3. Часть A08.3 — проверка идентичности полного набора

- [x] 3.1 Для одного шаблона написать тест на повтор UUID между секциями, включая выключенный элемент, и на допустимый пустой/уникальный набор. По каждому новому сценарию подтвердить Red, реализовать минимум проверки до Green, отрефакторить и повторить. Проверка: `duplicate_element_id` содержит обе позиции, вход не меняется; результат записан.
- [x] 3.2 Для локального набора роутера последовательно проверить повтор между локальными элементами, совпадение с исключённым/выключенным общим и коллизию после добавления нового общего элемента. Для каждого: тест → Red → минимальная логика → Green → рефакторинг/повтор; проверить обоих владельцев/позиции и отсутствие неявного превращения в переопределение, записать результаты.
- [x] 3.3 Проверить независимые владельцы, замену исходной записи кандидатом и отказ при неверном UUID/контексте в переданном наборе отдельными циклами «тест → Red → минимум → Green → рефакторинг/повтор». Критерий — независимые одинаковые UUID допустимы, нормальное обновление не становится дубликатом, ошибка не раскрывает неверный ID; результаты в отчёте.

## 4. Часть A08.4 — гарантии границ и интеграционная приёмка

- [x] 4.1 Для `Persisted drafts do not imply assembly readiness` проверить сохранение пустых/отсутствующих дефолтов, вложенного `null`, частичных/ошибочных ссылок и невалидной семантики определений. Новое поведение разрабатывать через тест → Red → минимум → Green → рефакторинг/повтор; если тест уже зелёный после прежних циклов, честно записать регрессионную проверку без придуманного Red. Критерий — данные восстановлены без исправления, нет вызова полной сборки или Xray.
- [x] 4.2 Покрыть `Element storage errors are atomic and safe`: модификация результата не меняет входы; ошибки UUID, содержимого, дефолта и оболочки не содержат синтетических значений или сырого JSON; результат при отказе отсутствует. Для каждого недостающего поведения пройти тест → Red → минимум → Green → рефакторинг/повтор, существующие гарантии отметить как регрессию; записать результаты и проверить направление зависимостей ядра и адаптера по импортам.
- [x] 4.3 Выполнить интеграционный сценарий в памяти: сохранение с новыми объявлениями → чтение → изменение типа/дефолта → удаление ссылки с сохранением определения → проверка кандидатного набора → повторное чтение. Сопоставить итог с дельтами: точный JSON, стабильный UUID, сохранённые настройки, проверенный набор; записать фактический результат без заявления о БД, UI и готовой сборке.

## 5. Документация и итоговые проверки

- [x] 5.1 Обновить `docs/configuration-model.md`, `docs/prototype.md`, `docs/roadmap.md`, README и описание текущего состояния в AGENTS.md: отразить фактически реализованные части A08, автообъявление, начальные настройки, сохранение неиспользуемых определений и границу B01/B02. Проверить ссылки, JSON-примеры и отсутствие частных данных; не менять исторические результаты A05/A07 и не выдавать будущие UI-состояния за проверенный макет.
- [x] 5.2 Выполнить общие проверки по `docs/ci.md`: форматирование, `go test ./... -count=1`, `go vet ./...`, race и сборки Linux AMD64/ARM64 без cgo. В `verification.md` записать команды, фактические результаты и ограничения окружения; отдельно проверить `git diff --check` и строгую валидацию change. Повторять зелёные проверки только после относящихся к ним изменений.
- [x] 5.3 Сверить все критерии #9 с тестами/отчётом: ID, определения, неизвестные поля и исходный текст, дубликаты по области, документация и ограничения. Проверки полной сборки, перечисленные в сценариях как будущий контракт A05, явно оставить B01/B02; не объявлять их реализованными по результатам A08.
- [x] 5.4 При завершении реализации синхронизировать обе дельты с основными specs и архивировать change штатным OpenSpec workflow; проверить валидность основных specs и ссылки после переноса. Отдельно проверить состояние #9 и закрыть только при всей выполненной приёмке; в итоговом отчёте указать архивный путь и фактический статус issue либо причину, почему она остаётся открытой.
