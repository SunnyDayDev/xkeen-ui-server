# Проверка B07 · 2026-09-23

## Выполнено

- Предметное ревью сценариев: начальный и персональный порядок, вставка по числовому индексу, удаление, пакетное изменение шаблона, исключение, возврат и безопасные ошибки. Уточнено, что роутер по умолчанию следует перестановке шаблона только при отсутствии персонального порядка; добавлен пакетный пример.
- Макет: через MCP Pencil обновлены экран 06, экран 08 и отдельное состояние ошибки сохранения. Снимки всех трёх экранов просмотрены; проверка узлов Pencil не обнаружила обрезанных элементов. После сохранения пользователем `git status` показывает изменённый файл `.pen`; пункт 2.1 выполнен. Интерфейс сервиса не реализован.
- Документы: модель конфигурации, описание прототипа, карта экранов и план разработки сверены с дельтой. Примеры `A, B, C → A, X, B, C` и `C, A, B → C, X, A, B` сохраняют числовой индекс 1.
- `openspec validate define-router-rule-order --strict` — успешно.
- Основная спецификация `configuration-rule-order` создана из дельты: 6 требований и 23 сценария. `openspec validate --specs` — 7 спецификаций прошли, ошибок нет; предупреждения о длине отдельных требований носят информационный характер.
- Локальные Markdown-ссылки и пробелы на концах строк в десяти затронутых Markdown-файлах — ошибок нет. `git diff --check` — успешно. Поиск частных адресов, локальных путей и значений секретов в затронутых материалах — совпадений нет.
- Сверка с основной `configuration-content-inheritance`: B06 выбирает содержимое без порядка, проверяет ссылки до исключений и сохраняет персональные данные при исчезновении источника. Дельта B07 не меняет эти правила. Сценарий сохранения передаёт дальнейшие вопросы атомарности и конкурентных правок C05–C07.

## Итоговое состояние и границы

- Тесты и код B08, хранилище C04, публикация C05–C07 и рабочий UI отсутствуют; поведенческих Red/Green для порядка в B07 нет.
- [Issue B07 #25](https://github.com/SunnyDayDev/xkeen-ui-server/issues/25) закрыта после сверки всех шести критериев приёмки; обзорная issue #1 остаётся открытой.
- Основная спецификация синхронизирована; change архивирован в `openspec/changes/archive/2026-09-23-define-router-rule-order/` с `--skip-specs`, чтобы не применять уже перенесённую дельту повторно. Все 10 пунктов tasks выполнены.
