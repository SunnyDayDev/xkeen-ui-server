## 1. Привязка и исходная проверка

- [x] 1.1 До реализации завести отдельную issue B05 с критериями proposal/specs, зависимостями #9/#18/#19 и ссылкой на change; обновить ссылку в proposal и проверить, что issue открыта. Не закрывать обзорную #1.
- [x] 1.2 Подтвердить исходный `go test ./internal/configuration/... -count=1`; в `verification.md` завести соответствие всех 23 сценариев дельты и переданных сценариев B03 будущим тестам. Зафиксировать исходный результат; это не Red нового поведения.

## 2. Выбор содержимого одного элемента

- [x] 2.1 Для `Full replacement does not merge missing fields` написать тест через `SelectElementContent` по интерфейсу design, получить поведенческий Red с минимальными объявлениями, затем реализовать минимальную модель/выбор для Green; рефакторинг и повторная целевая проверка, записать результаты в verification.md.
- [x] 2.2 По `Invalid source fails even for literal replacement` и `Unknown mode is rejected` сначала добавить тесты UUID, контекстов, обоих несовпадений и неизвестного режима, включая приоритет перед неверным JSON; подтвердить Red, реализовать Green и повторно проверить. Если извлекается общая проверка B04, выполнить существующие тесты наследования до/после и подтвердить неизменность кодов/контекстов.
- [x] 2.3 По `Selection routes absent and variable settings to inheritance` проверить отсутствие записи, пустые и заполненные значения и ошибки B04 через новую границу; тесты → Red → минимальная композиция с B04 → Green/Refactor. Первоначальный Green у повторно используемого поведения честно отметить; не менять B04 ради искусственного Red.
- [x] 2.4 По сценариям `Replacement strings remain literal`, `Root null is content rather than absence`, `Nested null remains literal`, `Non-object roots are not wrapped` сначала написать проверки строк/ключей, всех корневых типов, вложенного `null`, точных чисел и массивов; Red → минимальная логика → Green/Refactor. Проверить неизменный запрет `null` в B01/B02 и отсутствие добавленной `_xkeenUiId`; первоначальные Green отметить.
- [x] 2.5 По `Invalid replacement has no fallback` и `Duplicate replacement keys are rejected` сначала добавить случаи nil/пустого ввода/пробелов, повреждённого JSON, двух корней, невалидного UTF-8 и повторов ключей в корне/вложенности/экранировании; Red → синтаксическая проверка → Green/Refactor. Проверить `invalid_json`/`duplicate_json_key`, путь/смещение и отсутствие fallback.
- [x] 2.6 По `Template variable errors do not enter replacement`, `Replacement ignores broken inactive content`, `Template changes cannot alter a replacement` написать последовательные тесты смены JSON/определений/дефолтов и некорректных неактивных значений; Red → выбор только активной ветви → Green/Refactor, либо записать первоначальный Green. Проверить сохранность всех входов.
- [x] 2.7 По `Replacement diagnostics and buffers are safe` сначала проверить независимость входного/выходного JSON и отсутствие маркеров данных/невалидных ID во всей диагностике; Red → минимальное исправление → Green/Refactor. Проверить владельца/позицию/UUID и точность чисел, не ограничиваясь строковым представлением ошибки.

## 3. Переходы состояния, возврат и сброс

- [x] 3.1 По `Updating replacement preserves inactive values` и базовому переключению B03 написать тесты `TransitionContentOverride`: активация и повторная замена с явно переданным JSON, сохранение Values без проверки по шаблону. Red → минимальный переход → Green/Refactor; подтвердить источник, полную замену и сохранность неверных неактивных буферов.
- [x] 3.2 По `Switch and return keep values after template evolution` и сценариям B03 `Values return after switching back`, `Type change is diagnosed on return`, `Removed definition leaves a visible error`, `New required value is missing on return` сначала написать последовательные тесты переход → выбор. Red → возврат независимого кандидата → Green/Refactor; подтвердить возвращённые значения либо `type_mismatch`/`unknown_variable`/`missing_value` без потери кандидата.
- [x] 3.3 По `Reset succeeds before incomplete assembly` и сценариям B03 `Reset uses the current default`, `Reset can leave an incomplete draft`, `Reset does not resurrect retained values` сначала проверить сброс из обоих режимов, отсутствие/повторный сброс и последующие переключения. Red → минимальный сброс → Green/Refactor; успешный `nil, nil` отличается от отказа, удалённые данные не восстанавливаются.
- [x] 3.4 По `Activating replacement requires fresh explicit content` и `Invalid transitions preserve the prior state` сначала проверить отсутствие JSON, явный `null`, синтаксические ошибки, неизвестные действие/режим и неверный источник; Red → проверки перехода → Green/Refactor. Возврат и сброс из неготового содержимого не требуют его сборки; ошибочный переход не возвращает кандидат.
- [x] 3.5 По `Discarding a candidate cancels only the edit`, B03 `Cancel leaves saved state intact` и общему инварианту независимости сначала проверить отказ от кандидата, глубокие копии карты/буферов и изменения входов после вызова. Red → исправление владения → Green/Refactor. Подтвердить, что операции принимают/возвращают только состояние содержимого; внешние порядок, исключение и другие элементы не меняются. Интеграцию исключений оставить B06.

## 4. Проверка списка источников

- [x] 4.1 По `Repeated bindings identify both positions` сначала написать тест `ValidateOverrideSources` с повторным источником в двух позициях; Red → минимальный валидатор → Green/Refactor. Проверить `duplicate_override`, обе позиции, UUID, отсутствие выбора последней записи и неизменность списка.
- [x] 4.2 По `Empty and distinct bindings are valid` сначала проверить пустой список, разные UUID, независимые вызовы для разных роутеров; Red → минимальная логика → Green/Refactor либо честный первоначальный Green. Успех не должен обещать существование источников или сборку полного набора.
- [x] 4.3 По `Invalid binding is not treated as absent` сначала проверить владельцев даже при пустом списке, позиции, UUID и TemplateID; Red → проверки → Green/Refactor. Подтвердить коды, приоритет по design и отсутствие невалидных полей в диагностике; режимы/пустые значения не исключают записи из проверяемого списка.

## 5. Общая проверка и документация

- [x] 5.1 Выполнить `go test ./... -count=1`, `go test -race ./... -count=1`, `go vet ./...`, проверить форматирование затронутых Go-файлов и сборки Linux amd64/arm64 с `CGO_ENABLED=0`. Записать фактические команды/результаты в verification.md; сбой окружения не считать Red. Подтвердить отсутствие новых зависимостей и I/O ядра и регрессий Element/elementjson/B01/B02/B04.
- [x] 5.2 После реализации обновить README, AGENTS и docs/architecture.md, docs/configuration-model.md, docs/prototype.md, docs/roadmap.md: доступный объём B05, допустимость `null`, переходы/сброс, границы B06/БД/UI. Проверить ссылки, JSON-примеры и отсутствие частных данных; не объявлять макеты, Xray или устройство проверенными.
- [x] 5.3 Сверить все сценарии дельты и применимые сценарии B03 с тестами и критериями issue; выполнить строгую OpenSpec-валидацию и заполнить verification.md фактическими Red/Green/регрессиями. После приёмки отдельно проверить синхронизацию/архивирование change и закрытие B05; в итоговом отчёте явно указать оба статуса, не закрывать частичную реализацию и обзорную #1.
