## 1. Подготовка к первому тесту

- [x] 1.1 Подготовить Go 1.27.1, создать `go.mod` по design и исключить `.build/` из Git; проверить `go version`, параметры модуля и отсутствие сторонних runtime-зависимостей. Добавлять только объявления, необходимые для компиляции первого теста, без реализации поведения.

## 2. HTTP-контракт

- [x] 2.1 Red: написать тест `GET /healthz` по сценарию Health without product configuration; запустить его на минимальной заглушке и зафиксировать падение по статусу/заголовку/JSON, а не по ошибке сборки.
- [x] 2.2 Green → Refactor: реализовать успешный health-ответ, проверить новый тест, улучшить структуру handler без изменения поведения и повторить проверку.
- [x] 2.3 Red: добавить табличные проверки Unsupported method or route для HEAD/POST и неизвестных путей, включая Allow и последующий успешный GET; записать фактическое падение до соответствующей логики.
- [x] 2.4 Green → Refactor: реализовать 405/404, пройти все HTTP-тесты и повторить их после рефакторинга; проверить отсутствие продуктовых и тестовых маршрутов сверх `/healthz`.

## 3. Контракт параметров запуска

- [x] 3.1 Red: написать проверки default-address, IPv4/IPv6, порта 0, неверных аргументов и `--help` согласно спецификации; до реализации parser зафиксировать падения по ожидаемым результатам.
- [x] 3.2 Green → Refactor: реализовать разбор аргументов и помощь без открытия listener, выполнить проверки раздела 3 и существующие HTTP-тесты; повторить проверки после изменения структуры parser.

## 4. Жизненный цикл HTTP

- [x] 4.1 Red: через контракт `Serve` написать проверки остановки по отмене контекста, освобождения порта, уже отменённого контекста и неожиданной ошибки Accept; подтвердить падения по поведению и гарантировать очистку тестовых ресурсов.
- [x] 4.2 Green → Refactor: реализовать старт/остановку и возврат ошибок, пройти проверки раздела 4.1 и предыдущие тесты; проверить направление зависимости `cmd` → `internal/server` и повторить тесты после рефакторинга.
- [x] 4.3 Red: добавить проверки Finish an in-flight request и Shutdown deadline exceeded с управляемыми handlers, подтверждением начала запроса и коротким внутренним timeout; получить падения до добавления graceful/deadline-логики, не добавляя тестовые маршруты в сервер.
- [x] 4.4 Green → Refactor: реализовать отдельный контекст ожидания, завершение активного запроса и принудительное закрытие по сроку; пройти проверки результата/ошибки/закрытия соединения, очистки ресурсов и весь существующий набор после рефакторинга.

## 5. Настоящий процесс и одна команда запуска

- [x] 5.1 Red: до wiring в `main` добавить процессные проверки Explicit listener and allocated port, Address already occupied, Invalid invocation, Help without starting и Stop an idle server для SIGINT/SIGTERM; запустить компилируемый бинарник-заглушку и записать падения по контракту с ограниченным ожиданием и очисткой процессов.
- [x] 5.2 Green → Refactor: подключить parser, listener, запись фактического адреса, сигналы и отображение ошибок в коды 0/1/2; пройти процессные тесты и предыдущие проверки, затем повторить их после рефакторинга composition root. Проверить IPv6-запуск при наличии IPv6, иначе записать причину пропуска.
- [x] 5.3 Red: добавить проверку wrapper одной команды с передачей `--listen`, остановкой после readiness и кодом завершения; подтвердить поведенческое падение на минимальном скрипте-заглушке до реализации сборки/exec.
- [x] 5.4 Green → Refactor: реализовать `scripts/run-server.sh` со сборкой и `exec`, пройти тест wrapper и выполнить default-запуск из корня при свободном 8080; подтвердить health, штатную остановку и код 0, повторить проверки после рефакторинга скрипта.

## 6. Целевая совместимость и результат

- [x] 6.1 Выполнить `go test ./...` и `go vet ./...` на Go 1.27.1, кросс-сборку `CGO_ENABLED=0 GOOS=linux GOARCH=arm64` по design; проверить ELF aarch64 и отсутствие обязательного динамического загрузчика/библиотек, записать команды, исходы и размер бинарника.
- [x] 6.2 Выполнить Entware lifecycle probe в отдельном ARM64-контейнере существующего lab-образа: старт, loopback health, SIGTERM/exit 0 и повторный запуск. Проверить удаление временного контейнера и отсутствие изменений рабочих volumes; при недоступной среде оставить задачу незавершённой.
- [x] 6.3 Создать `docs/server-skeleton.md` с командами установки выбранного toolchain, запуска из исходников/бинарника, тестов и воспроизводимой Entware-пробы; обновить README, архитектуру и статус A03. В change добавить `verification.md` с фактическими Red/Green, итоговыми проверками, версиями и ограничениями. Проверить ссылки, примеры JSON, отсутствие частных данных, `git diff --check` и `openspec validate add-server-skeleton --strict`; реализацию считать завершённой только после выполнения обязательных проверок.
