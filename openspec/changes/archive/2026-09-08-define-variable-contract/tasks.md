## 1. Ревью и завершение контракта A05

- [x] 1.1 Провести ревью таблицы статусов решений в design: область запрета null, дефолт при пустой строке, Unicode trim, имена, экранирование, однопроходность и диагностики. Готово: результат ревью отражён в артефактах, предложенные детали не обозначены согласованными без подтверждения.
- [x] 1.2 Сверить окончательный контракт с критериями issue #6: точное имя и точка, пять типов, дефолты, обязательность, пустые строки, null, буквальные ссылки и частичная интерполяция. Готово: каждый пункт имеет сценарий с однозначными входами и ожидаемым результатом; `openspec validate define-variable-contract --strict` проходит.

## 2. Согласование предметной документации

- [x] 2.1 После ревью обновить раздел переменных `docs/configuration-model.md` по утверждённому контракту и дать ссылку на спецификацию, сохранив отметку об отсутствии реализации. Готово: больше нет противоречащих контракту открытых вопросов о null, trim и интерполяции; ссылки и JSON-примеры проверены.
- [x] 2.2 Обновить раздел 5 и строку переменных в разделе 15 `docs/prototype.md`, а также результат A05 в `docs/roadmap.md`; отдельно сохранить будущие вопросы эволюции определений и состояние UI. Готово: документы совпадают с контрактом и не выдают макеты, A06/B01/B02 или новое поведение форм за реализованные возможности.

## 3. Проверка передачи в последующие задачи

- [x] 3.1 Сверить распределение сценариев между A06, B01 и B02 в design с roadmap. Готово: все требования назначены дальнейшей задаче, UI/БД/API и реализация кода не добавлены в A05, доменные правила проверяемы без сервера и роутера.
- [x] 3.2 Проверить, что план передачи предусматривает отдельный цикл для каждого малого сценария A06/B01/B02: контрактный тест → запуск и подтверждение Red из-за отсутствующего поведения → минимальная логика и Green → рефакторинг и повторные проверки с фактическим отчётом. Готово: этот порядок сохранён в design для переноса в будущие tasks; подготовка A05 не выдаётся за выполненные Red/Green.

## 4. Итоговая проверка и завершение

- [x] 4.1 Проверить весь согласованный комплект: строгая валидация change, JSON-примеры, локальные ссылки, отсутствие частных данных и `git diff --check`. Готово: фактические команды и результаты записаны в отчёте завершения; ограничения и невыполненные проверки перечислены отдельно.
- [x] 4.2 При завершении A05 синхронизировать принятую capability с основной spec и архивировать change по workflow завершения; проверить критерии issue #6 и закрыть её только при полном выполнении. Готово: в итоговом отчёте отдельно указаны архивирование и фактический статус issue, а A06/B01/B02 остаются будущей реализацией.

## 5. Результат подготовки предложения

2026-09-08: созданы все четыре артефакта. `openspec validate define-variable-contract --strict` завершился успешно. Проверены наличие целей пяти локальных ссылок, синтаксис двух JSON-блоков, наличие WHEN/THEN во всех 22 сценариях семи требований и отсутствие локальных пользовательских путей/типичных префиксов токенов в артефактах. `git diff --check` завершился с кодом 0; поскольку новые файлы ещё не отслеживаются, это не самостоятельное подтверждение их форматирования.

Эти проверки относятся к подготовленному предложению, не закрывают будущую итоговую сверку после ревью и обновления документации. Тесты поведения и Red/Green не запускались: реализации нет. На этапе подготовки issue #6 была открыта, change ещё не был архивирован. Итог завершения приведён в verification.md.
